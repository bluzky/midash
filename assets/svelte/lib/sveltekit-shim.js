// Shim for SvelteKit modules used by third-party libraries (e.g. lightweight-charts-svelte).
// This app is CSR-only, so browser is always true.
export const browser = true
export const dev = false
export const building = false
